{
  "name": "@multi-op/core",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@multi-op/shared": "workspace:*",
    "@multi-op/database": "workspace:*"
  },
  "devDependencies": {
    "electron": "^43.1.1",
    "typescript": "^7.0.2"
  }
}
